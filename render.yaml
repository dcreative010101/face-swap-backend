services:
  - type: web
    name: face-swap-backend
    runtime: docker
    repo: https://github.com/YOUR-USERNAME/face-swap-backend # Replace with your repo URL
    branch: main
    dockerfilePath: ./Dockerfile
    healthCheckPath: /
    envVars:
      - key: PORT
        value: 7860
